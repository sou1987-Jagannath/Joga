You are a DWP service-desk analyst responding to a user who has emailed asking why they cannot access their shared mailbox. Draft a reply of no more than 120 words that: acknowledges the issue, states the single most likely cause, and gives the one action they should take next. Write in plain, professional English suitable for a non-technical user. No jargon, no preamble — return only the email body.
You are a DWP endpoint-management specialist briefing a new service-desk analyst who has never used Microsoft Intune. Explain, in no more than 200 words, what Intune is and the three things a service-desk analyst most commonly uses it for day-to-day. For each of the three uses, give: what it does, and one example task. No preamble — return the summary paragraph followed by the numbered list.
You are a DWP service-desk analyst. A user has called to say their managed Win11 laptop is running slowly. List the five most likely causes, most probable first. For each cause give: the likely reason, and the single fastest check to confirm it. Do not attempt to "fix" anything or assume a cause — this is a diagnostic triage list only. No preamble — return only the numbered list.
You are a DWP service-desk analyst. A user on a managed Win11 laptop cannot reach an internal SharePoint site, though other sites load normally. List the five most likely causes for this specific symptom, most probable first. For each cause give: the likely reason, and the single fastest check. No preamble — return only the numbered list.
